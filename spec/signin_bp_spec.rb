# require_relative 'page/signin_bp.rb'

# describe 'Login Spec' do
#     context 'Basic Navigation' do
#         it 'verifies sign in basic view components' do

#             basic_view_logo = BasicViewLogo.new
#             byebug
#             expect(basic_view_logo).to eq(logo)

            # # basic_view_text = BasicViewText.new 
            # # expect(basic_view_text).to eq(text)
      
            # basic_view1_text = BasicView1Text.new 
            # # expect(basic_view1_text).to eq(text)
      
            # dropdown = Dropdown.new 
            # # expect(dropdown).to eq(dropdown)
      
            # continue_option = ContinueOption.new
            # # expect(continue_option).to eq(continue_button)
      
            # hyperlink_text = HyperlinkText.new
            # # expect(hyperlink_text).to eq(link_text)
      
            # hyperlink_text = HyperlinkText.new
            # # expect(bold_text).to eq(bold_text)
      
            # sign_in_link = SignInLink.new 
            # # expect(sign_in_link).to eq(signin_link)
      
            # normal_link = NormalLink.new
            # # expect(normal_link).to eq(normal_link)
      
            # mini_text = MiniText.new      
            # # expect(mini_text).to eq(mini_text)
#         end
#     end
# end

